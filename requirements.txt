opencv-python>=4.8
numpy>=1.24
Pillow>=10.0
customtkinter>=5.2

# Basler SDK - needs the Pylon runtime installed on the machine.
# App runs without it (webcam -> synthetic fallback) for development.
pypylon>=3.0
